<!DOCTYPE>
<html>
<head>
<title>Email and Capital tickets schedules | Marketing Department | Ottawa Senators</title>
<!--[if lt IE 9]>
	    <script src="http://html5shim.googlecode/svn/trunk/html5.js" type="text/javascript"></script>
	  <![endif]-->
<%= stylesheet_link_tag    "application", :media => "all" %><%= javascript_include_tag "application" %><%= csrf_meta_tags %>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container"> <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </a> <%= link_to 'Schedules', '../main', :class => 'brand' %>
      <div class="nav-collapse">
        <ul class="nav">
          <% if current_user %>
          <li><%= link_to 'Email schedule', email_schedules_path %></li>
          <li><%= link_to 'Spotlight schedule', captixes_path %></li>
          <li><a heref="#">Logged in as <%= current_user.email %></a></li>
          <li><%= link_to("Logout", :controller => "sessions", :action => 'destroy') %> </li>
          <% else %>
          <li><%= link_to "Log In", login_path %></li>
          <% end %>
          <li><%= link_to "About", '../about' %></li>
        </ul>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <% flash.each do |name, msg| %>
  <div class="alert alert<%= name == :notice ? "-success" : "-error" %>"> <%= msg %> </div>
  <% end %>
  <%= yield %> </div>
</html>
